<%= render partial: 'layouts/errors', locals: {object: @loan} %>

<% @loans.each do |l| %>
  <ul>
    <%=link_to l.tool.name, tool_path(l.tool) %> was loaned on: <%=link_to l.loaned_on.strftime("%b %e, %l:%M %p"), loan_path(l.borrower)%> to <%= l.borrower.name %>
  </ul>
<% end %>
